# 翻译补译队列（Incremental translation TODO）

本文件由 GitHub Actions 自动维护：每当上游 `cloudflare/kumo` 的文档内容被改动，
`Sync upstream and deploy` workflow 会把**本轮涉及的文档文件**追加到下面的清单。

补译原则：**只增量翻译清单里列出的文件，不整站重翻。** 翻译完成后把对应条目删除并提交，
站点会随之自动更新（无需手动部署）。

清空清单 = 站点内容全部为最新汉化状态。

## 待补译文件

（当前无待处理项）