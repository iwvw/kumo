---
layout: ~/layouts/MdxDocLayout.astro
title: "Radio"
description: "一种允许用户从一组选项中选择一项的控件。始终在 Radio.Group 中使用。"
sourceFile: "components/radio"
baseUIComponent: "radio-group"
---

import ComponentExample from "~/components/docs/ComponentExample.astro";
import ComponentSection from "~/components/docs/ComponentSection.astro";
import PropsTable from "~/components/docs/PropsTable.astro";
import {
  RadioBasicDemo,
  RadioDefaultDemo,
  RadioHorizontalDemo,
  RadioDescriptionDemo,
  RadioErrorDemo,
  RadioDisabledDemo,
  RadioControlPositionDemo,
  RadioCardDemo,
  RadioCardControlStartDemo,
  RadioCardHorizontalDemo,
  RadioRichLabelDemo,
  RadioLegendSrOnlyDemo,
  RadioLegendCustomDemo,
  RadioTypedValueDemo,
} from "~/components/demos/RadioDemo";

{/* Hero Demo */}

<ComponentSection>
  <ComponentExample demo="RadioBasicDemo">
    <RadioBasicDemo client:visible />
  </ComponentExample>
</ComponentSection>

{/* Installation */}

<ComponentSection>

## 安装

### 桶式导出

```tsx
import { Radio } from "@cloudflare/kumo";
```

### 细粒度导入

```tsx
import { Radio } from "@cloudflare/kumo/components/radio";
```

</ComponentSection>

{/* Usage */}

<ComponentSection>

## 用法

```tsx
import { Radio } from "@cloudflare/kumo";

export default function Example() {
  return (
    <Radio.Group legend="Choose an option" defaultValue="a">
      <Radio.Item label="Option A" value="a" />
      <Radio.Item label="Option B" value="b" />
    </Radio.Group>
  );
}
```

</ComponentSection>

{/* Examples */}

<ComponentSection>

## 示例

### 默认布局（垂直）

<p>
  单选组默认垂直排列。每个单选按钮的标签
  显示在右侧。
</p>
<ComponentExample demo="RadioDefaultDemo">
  <RadioDefaultDemo client:visible />
</ComponentExample>

### 水平布局

<p>
  使用 `orientation="horizontal"` 实现内联布局。空间不足时
  项目会自动换行。
</p>
<ComponentExample demo="RadioHorizontalDemo">
  <RadioHorizontalDemo client:visible />
</ComponentExample>

### 带描述

<p>使用 `description` 属性在单选项目下方添加辅助说明文字。</p>
<ComponentExample demo="RadioDescriptionDemo">
  <RadioDescriptionDemo client:visible />
</ComponentExample>

### 控件位置

<p>使用 `controlPosition="end"` 将标签放在单选按钮左侧。</p>
<ComponentExample demo="RadioControlPositionDemo">
  <RadioControlPositionDemo client:visible />
</ComponentExample>

### 卡片式单选

<p>
  在组上使用 `appearance="card"`，可将每个选项显示为可选卡片。
  再配合每个项目的 `description` 属性，可获得更丰富的内容。
</p>
<ComponentExample demo="RadioCardDemo">
  <RadioCardDemo client:visible />
</ComponentExample>

### 卡片式单选（控件在左侧）

<p>
  在卡片式单选组上使用 `controlPosition="start"`，可将单选控件放在标签
  和描述的左侧。
</p>
<ComponentExample demo="RadioCardControlStartDemo">
  <RadioCardControlStartDemo client:visible />
</ComponentExample>

### 富标签内容

<p>
  `Radio.Item` 的 `label` 属性接受 React 节点，因此可以在文字旁边嵌入图标、
  徽标或其他标记。
</p>
<ComponentExample demo="RadioRichLabelDemo">
  <RadioRichLabelDemo client:visible />
</ComponentExample>

### 卡片式单选（水平）

<p>
  将 `appearance="card"` 与 `orientation="horizontal"` 结合，
  可实现并排的卡片布局。
</p>
<ComponentExample demo="RadioCardHorizontalDemo">
  <RadioCardHorizontalDemo client:visible />
</ComponentExample>

### 错误状态

<p>使用 `error` 属性在组级别显示校验错误。</p>
<ComponentExample demo="RadioErrorDemo">
  <RadioErrorDemo client:visible />
</ComponentExample>

### 禁用状态

<p>可以禁用整个组或禁用一个或多个项目。</p>
<ComponentExample demo="RadioDisabledDemo">
  <RadioDisabledDemo client:visible />
</ComponentExample>

### 视觉隐藏的图例

<p>
  使用带 `className="sr-only"` 的 `Radio.Legend`，可以在视觉上隐藏图例的同时
  保持其为屏幕阅读器可读。当单选组已由父级 `Field` 或标题提供了标签，
  再显示图例会形成冗余标签时，
  这一用法很有用。
</p>
<ComponentExample demo="RadioLegendSrOnlyDemo">
  <RadioLegendSrOnlyDemo client:visible />
</ComponentExample>

### 自定义图例样式

<p>
  `Radio.Legend` 接受 `className`，可完全控制图例的呈现方式。当你需要
  自定义排版、颜色或布局时，请用它替代
  `legend` 字符串属性。
</p>
<ComponentExample demo="RadioLegendCustomDemo">
  <RadioLegendCustomDemo client:visible />
</ComponentExample>

### 类型化值

<p>
  `Radio.Group` 和 `Radio.Item` 支持泛型类型。传入类型参数（例如 `<number>` 或枚举）
  可以约束两个子组件上 `value` 的类型，以及 `Radio.Group` 上 `defaultValue` 和
  `onValueChange` 的类型。省略时默认为
  `string`。
</p>
<ComponentExample
  vrSection="radio-typed-value"
  vrTitle="Radio Typed Value"
  code={`enum ThemeType {
    dark = "dark",
    light = "light",
    system = "system",
}

export function RadioTypedValueDemo() {
    const [pageSize, setPageSize] = useState<number>(10);
    const [theme, setTheme] = useState<ThemeType>(ThemeType.system);
    return (
      <div className="grid grid-cols-2 gap-6">
        <Radio.Group<number>
          legend="Items per page"
          value={pageSize}
          onValueChange={setPageSize}
        >
          <Radio.Item<number> label="10" value={10} />
          <Radio.Item<number> label="25" value={25} />
          <Radio.Item<number> label="50" value={50} />
        </Radio.Group>
        <Radio.Group<ThemeType>
          legend="Theme"
          value={theme}
          onValueChange={setTheme}
        >
          <Radio.Item<ThemeType> label="Light" value={ThemeType.light} />
          <Radio.Item<ThemeType> label="Dark" value={ThemeType.dark} />
          <Radio.Item<ThemeType> label="System" value={ThemeType.system} />
        </Radio.Group>
      </div>
    );
}`}
>
  <RadioTypedValueDemo client:visible />
</ComponentExample>

</ComponentSection>

{/* API Reference */}

<ComponentSection>

## API 参考

### Radio.Group

<p>单选按钮的容器，支持图例、描述和错误提示。</p>
<PropsTable component="Radio.Group" />

### Radio.Legend

<p>
  可组合的 Radio.Group 图例子组件。接受 `className` 以完全控制样式
  （例如 `className="sr-only"` 实现视觉隐藏）。当你需要自定义图例样式时，
  用它替代 `legend` 字符串属性。
</p>
<PropsTable component="Radio.Legend" />

### Radio.Item

<p>Radio.Group 中的单个单选按钮。</p>
<PropsTable component="Radio.Item" />

</ComponentSection>

{/* Accessibility */}

<ComponentSection>

## 无障碍

  <div class="space-y-4 text-sm">
    <div>

      ### 语义化 HTML

      <p>
        Radio.Group 使用语义化的 `<fieldset>` 和 `<legend>` 元素，实现正确的分组和屏幕阅读器播报。
      </p>
    </div>
    <div>

      ### 键盘导航

      <p>
        使用 <kbd class="rounded bg-kumo-control px-1.5 py-0.5">Arrow Up/Down</kbd> 或 <kbd class="rounded bg-kumo-control px-1.5 py-0.5">Arrow Left/Right</kbd> 在选项间移动。<kbd class="rounded bg-kumo-control px-1.5 py-0.5">Space</kbd> 选中当前聚焦的选项。<kbd class="rounded bg-kumo-control px-1.5 py-0.5">Tab</kbd> 将焦点移入或移出单选组。
      </p>
    </div>
    <div>

      ### 屏幕阅读器

      <p>
        每个单选按钮都会播报其标签和选中状态。组的图例为所有选项提供上下文。
      </p>
    </div>

  </div>
</ComponentSection>
