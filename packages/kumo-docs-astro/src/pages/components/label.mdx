---
layout: ~/layouts/MdxDocLayout.astro
title: "Label"
description: "用于表单字段的标签组件，支持必填/可选标识和提示信息。"
sourceFile: "components/label"
---

import ComponentExample from "~/components/docs/ComponentExample.astro";
import ComponentSection from "~/components/docs/ComponentSection.astro";
import {
  LabelBasicDemo,
  LabelOptionalFieldDemo,
  LabelWithTooltipDemo,
  LabelReactNodeDemo,
  LabelFormMixedDemo,
  LabelStandaloneDemo,
} from "~/components/demos/LabelDemo";

{/* Hero Demo */}

<ComponentSection>
  <ComponentExample demo="LabelBasicDemo">
    <LabelBasicDemo client:visible />
  </ComponentExample>
</ComponentSection>

{/* Installation */}

<ComponentSection>

## 安装

### 桶式导入

```tsx
import { Label } from "@cloudflare/kumo";
```

### 子路径导入

```tsx
import { Label } from "@cloudflare/kumo/components/label";
```

</ComponentSection>

{/* Usage */}

<ComponentSection>

## 用法

### 与表单组件搭配使用（推荐）

  <p>
    通过 `required` 和 `labelTooltip` 属性，`Input`、`Select`、`Checkbox`、`Switch` 等表单组件会自动获得 Label 的各项功能。
  </p>

```tsx
import { Input } from "@cloudflare/kumo";

export default function Example() {
  return (
    <>
      {/* Optional field with "(optional)" text */}
      <Input label="Phone" required={false} placeholder="+1 555-0000" />

      {/* With tooltip */}
      <Input
        label="API Key"
        labelTooltip="Find this in your dashboard settings"
      />
    </>
  );
}
```

### 独立使用的 Label

<p>对于自定义表单布局，可直接使用 `Label` 组件。</p>

```tsx
import { Label } from "@cloudflare/kumo";

export default function Example() {
  return <Label tooltip="This field is mandatory">Username</Label>;
}
```

</ComponentSection>

{/* Examples */}

<ComponentSection>

## 示例

### 可选字段

<p>当 `required={false}` 时显示灰色“（可选）”文本。</p>
<ComponentExample demo="LabelOptionalFieldDemo">
  <LabelOptionalFieldDemo client:visible />
</ComponentExample>

### 带提示

<p>显示一个信息图标，悬停时提供额外的说明信息。</p>
<ComponentExample demo="LabelWithTooltipDemo">
  <LabelWithTooltipDemo client:visible />
</ComponentExample>

### ReactNode 标签内容

<p>标签支持 ReactNode 内容，可实现丰富的格式。</p>
<ComponentExample demo="LabelReactNodeDemo">
  <LabelReactNodeDemo client:visible />
</ComponentExample>

### 混合字段表单

<p>展示必填与可选字段并存的真实示例。</p>
<ComponentExample demo="LabelFormMixedDemo">
  <LabelFormMixedDemo client:visible />
</ComponentExample>

### 独立使用的 Label

<p>在自定义布局或非表单场景下直接使用 Label。</p>
<ComponentExample demo="LabelStandaloneDemo">
  <LabelStandaloneDemo client:visible />
</ComponentExample>

</ComponentSection>

{/* API Reference */}

<ComponentSection>

## API 参考

### Label 属性

<p>独立 Label 组件的属性：</p>
<div class="overflow-x-auto">
  <table class="w-full text-sm">
    <thead>
      <tr class="border-b border-kumo-hairline">
        <th class="px-4 py-3 text-left font-semibold">属性</th>
        <th class="px-4 py-3 text-left font-semibold">类型</th>
        <th class="px-4 py-3 text-left font-semibold">默认值</th>
        <th class="px-4 py-3 text-left font-semibold">描述</th>
      </tr>
    </thead>
    <tbody>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">children</td>
        <td class="px-4 py-3 font-mono text-xs">ReactNode</td>
        <td class="px-4 py-3 font-mono text-xs">-</td>
        <td class="px-4 py-3 text-xs">标签内容（必填）</td>
      </tr>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">showOptional</td>
        <td class="px-4 py-3 font-mono text-xs">boolean</td>
        <td class="px-4 py-3 font-mono text-xs">false</td>
        <td class="px-4 py-3 text-xs">
          显示灰色“（可选）”文本（仅当 required 为 false 时）
        </td>
      </tr>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">tooltip</td>
        <td class="px-4 py-3 font-mono text-xs">ReactNode</td>
        <td class="px-4 py-3 font-mono text-xs">-</td>
        <td class="px-4 py-3 text-xs">通过信息图标显示的提示内容</td>
      </tr>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">className</td>
        <td class="px-4 py-3 font-mono text-xs">string</td>
        <td class="px-4 py-3 font-mono text-xs">-</td>
        <td class="px-4 py-3 text-xs">额外 CSS 类</td>
      </tr>
    </tbody>
  </table>
</div>

### 表单组件 Label 属性

<p>
  以下属性适用于 Input、InputArea、Select、Checkbox、Switch、
  SensitiveInput 和 Combobox：
</p>
<div class="overflow-x-auto">
  <table class="w-full text-sm">
    <thead>
      <tr class="border-b border-kumo-hairline">
        <th class="px-4 py-3 text-left font-semibold">属性</th>
        <th class="px-4 py-3 text-left font-semibold">类型</th>
        <th class="px-4 py-3 text-left font-semibold">默认值</th>
        <th class="px-4 py-3 text-left font-semibold">描述</th>
      </tr>
    </thead>
    <tbody>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">label</td>
        <td class="px-4 py-3 font-mono text-xs">ReactNode</td>
        <td class="px-4 py-3 font-mono text-xs">-</td>
        <td class="px-4 py-3 text-xs">标签内容（启用 Field 包装器）</td>
      </tr>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">required</td>
        <td class="px-4 py-3 font-mono text-xs">boolean</td>
        <td class="px-4 py-3 font-mono text-xs">-</td>
        <td class="px-4 py-3 text-xs">
          为 false 时：显示“（可选）”文本，同时设置 HTML required 属性。
        </td>
      </tr>
      <tr class="border-b border-kumo-hairline">
        <td class="px-4 py-3 font-mono text-xs">labelTooltip</td>
        <td class="px-4 py-3 font-mono text-xs">ReactNode</td>
        <td class="px-4 py-3 font-mono text-xs">-</td>
        <td class="px-4 py-3 text-xs">
          通过标签旁的信息图标显示的提示内容
        </td>
      </tr>
    </tbody>
  </table>
</div>

</ComponentSection>

{/* Design Guidelines */}

<ComponentSection>

## 设计指南

  <div class="space-y-4 text-sm">
    <div>

      ### 何时使用可选标识

      <ul class="ml-4 list-disc space-y-1">
        <li>
          当大多数字段为必填时，为可选字段使用“（可选）”标识
        </li>
        <li>在同一表单内保持一致</li>
        <li>没有标识的默认字段，用户会默认视为必填</li>
      </ul>
    </div>
    <div>

      ### 何时使用提示

      <ul class="ml-4 list-disc space-y-1">
        <li>提供标签中无法容纳的补充上下文</li>
        <li>解释格式要求或校验规则</li>
        <li>为复杂字段链接到帮助文档</li>
        <li>保持提示内容简洁，最多 1-2 句</li>
      </ul>
    </div>
    <div>

      ### 无障碍

      <ul class="ml-4 list-disc space-y-1">
        <li>
          可选标识仅用于视觉展示，请使用 `required` 属性进行校验
        </li>
        <li>通过键盘聚焦信息图标即可访问提示内容</li>
        <li>屏幕阅读器在聚焦时会播报提示内容</li>
      </ul>
    </div>
  </div>
</ComponentSection>
