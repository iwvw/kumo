---
layout: ~/layouts/MdxDocLayout.astro
title: "Layer Dialog"
description: "一个严格响应的对话框组合组件，可在可滚动正文中提供固定的标题。"
sourceFile: "components/layer-dialog"
baseUIComponent: "drawer"
---

import ComponentExample from "~/components/docs/ComponentExample.astro";
import ComponentSection from "~/components/docs/ComponentSection.astro";
import PropsTable from "~/components/docs/PropsTable.astro";
import {
  LayerDialogActionDemo,
  LayerDialogAlertDemo,
  LayerDialogCancelDemo,
  LayerDialogCleanupDemo,
  LayerDialogFormDemo,
  LayerDialogInformationalDemo,
  LayerDialogMaxHeightDemo,
  LayerDialogNestedDemo,
  LayerDialogPendingDemo,
  LayerDialogSizeDemo,
  LayerDialogSplitActionDemo,
  LayerDialogTopAlignDemo,
} from "~/components/demos/LayerDialogDemo";

{/* Hero Demo */}

<ComponentSection>
  <ComponentExample demo="LayerDialogActionDemo">
    <LayerDialogActionDemo client:load />
  </ComponentExample>
</ComponentSection>

{/* Installation */}

<ComponentSection>

## 安装

### 桶式导入

```tsx
import { LayerDialog } from "@cloudflare/kumo";
```

### 子路径导入

```tsx
import { LayerDialog } from "@cloudflare/kumo/components/layer-dialog";
```

</ComponentSection>

{/* Usage */}

<ComponentSection>

## 用法

```tsx
import { Button, LayerDialog } from "@cloudflare/kumo";

export default function Example() {
  return (
    <LayerDialog.Root>
      <LayerDialog.Trigger
        render={(props) => <Button {...props}>Open settings</Button>}
      />
      <LayerDialog.Content>
        <LayerDialog.Title>Configure custom hostname</LayerDialog.Title>
        <LayerDialog.Description>
          Route requests for this hostname to your Worker.
        </LayerDialog.Description>
        <LayerDialog.Body>{/* form fields */}</LayerDialog.Body>
        <LayerDialog.Actions>
          <LayerDialog.Action onClick={save}>
            Save hostname
          </LayerDialog.Action>
        </LayerDialog.Actions>
      </LayerDialog.Content>
    </LayerDialog.Root>
  );
}
```

</ComponentSection>

<ComponentSection>
## 组合规则
`LayerDialog.Content` 恰好接受一个 `Title`、一个 `Body`、一个可选的 `Description` 和一个可选的 `Actions`。它会自动选择关闭 UI：

- 无 `Actions` 时：在标题栏中显示 X，且底部无操作栏。
- 有 `Actions` 时：移除 X，并显示含**关闭**或**取消**及一个主操作的底部操作栏。在移动端，它们保持在滚动正文下方、以发丝分隔线隔开；在桌面端，它们保留在固定底部操作栏中。

使用方不能混用这些布局，也不能添加无关的底部控件。`Actions` 接受一个 `Action`，它可以可选地暴露相关菜单项。只读内容用 X，用户必须提交变更时用 `Actions`，破坏性操作或重要确认用 `LayerDialog.Alert`。

`Description` 直接渲染在粘性标题栏内的标题下方，并成为对话框的可访问描述。没有它时，则由正文文本来描述对话框。
</ComponentSection>

<ComponentSection>
## 信息型对话框
`Title` 和 `Description` 始终位于带边框的正文表面内。正文滚动时标题栏保持可见，并提供唯一的自动 X 关闭操作。当正文滚动过顶部后，描述会折叠到标题下方，为内容腾出更多空间，滚回顶部时再展开。内容边缘遮罩用于提示溢出。
<ComponentExample demo="LayerDialogInformationalDemo"><LayerDialogInformationalDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 标准操作
添加 `Actions` 会恰好选择一个 Kumo 主操作和一个自动生成的**关闭**按钮。在移动端，操作保持在滚动正文下方、以发丝分隔线隔开；在桌面端，它们占据固定的底部操作栏。使用方不能添加无关的底部控件或额外 CTA。主操作接受 `variant="primary"`（默认）或 `variant="destructive"`。
<ComponentExample demo="LayerDialogActionDemo"><LayerDialogActionDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 原生表单

`Body` 和 `Actions` 是独立的插槽，因此原生 `<form>` 属于 `Body` 内部，而其提交按钮属于 `Actions`。给表单一个 ID，然后用其 HTML `form` 属性关联主操作。这保留了内置验证，并在没有 React 状态的情况下支持非受控表单字段。

<ComponentExample demo="LayerDialogFormDemo"><LayerDialogFormDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 多个主操作

通过 `LayerDialog.Action` 的 `menu` 属性传入相关操作。它会在操作旁渲染一个标准化的拆分按钮箭头。让自动关闭按钮保持独立。例如，**保存并部署** 可以暴露 **另存为草稿**，而不会把底部操作栏变成三个同级操作。

<ComponentExample demo="LayerDialogSplitActionDemo"><LayerDialogSplitActionDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 取消文案
当存在 `LayerDialog.Actions` 时，LayerDialog 会渲染一个用于关闭对话框的按钮。在移动端使用次要按钮样式，在桌面端底部操作栏中使用幽灵按钮样式。默认文案为“关闭”，对于 `LayerDialog.Alert` 则为“取消”。

仅当对话框需要更具体的文案时才使用 `dismissLabel`，例如“继续编辑”或“放弃更改”。该标签不改变按钮行为：它始终关闭对话框。
<ComponentExample demo="LayerDialogCancelDemo"><LayerDialogCancelDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 确认与破坏性操作
`LayerDialog.Alert` 遵循 Base UI 的 alert dialog：提供 `role="alertdialog"`、自动**取消**按钮、无 X、始终模态，并阻止背景与滑动关闭。Escape 仍然可以取消，符合 ARIA alert dialog 模式。
当确认操作不可逆时，为主操作传入 `variant="destructive"`。对于确认非破坏性但关键步骤的 Alert，保留默认的主操作样式。

<ComponentExample demo="LayerDialogAlertDemo"><LayerDialogAlertDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 嵌套对话框
当用户必须确认某个相关操作时，可以在父对话框的正文中嵌套一个 `LayerDialog.Root` 或 `LayerDialog.Alert`。每个对话框独立进行 portal 渲染；Base UI 会让嵌套抽屉在其父级之上保持激活，并在其关闭时将焦点返回给父级触发器。嵌套抽屉会在父级之上渲染自己的背景遮罩，因此父级保持完整但明显处于非激活状态。无需任何 z-index 覆盖。
<ComponentExample demo="LayerDialogNestedDemo"><LayerDialogNestedDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 进行中的操作
`dismissDisabled` 会在异步操作进行期间统一阻止所有用户触发的关闭操作。通过 `actionsRef.current.close()` 或受控 `open` 属性进行的程序化关闭仍然有效，因此成功的操作可以关闭对话框。唯一的主操作拥有独立的 loading 或 disabled 状态。
<ComponentExample demo="LayerDialogPendingDemo"><LayerDialogPendingDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 清理
将清理逻辑放在 `onOpenChange` 中，而不是放在关闭点击处理函数里，这样 X、关闭、Escape、背景和滑动关闭都能触发清理。
<ComponentExample demo="LayerDialogCleanupDemo"><LayerDialogCleanupDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 本地化

该对话框自带两处文案。可通过 `KumoLocaleProvider` 配置它们的全局默认值：

- `layerDialog.close` 用于自动 X 按钮和默认底部关闭按钮。默认值：“关闭”。
- `layerDialog.cancel` 用于 `Alert` 内默认底部关闭按钮。

```tsx
<KumoLocaleProvider
  translations={{
    layerDialog: { close: "Schließen", cancel: "Abbrechen" },
  }}
>
  <App />
</KumoLocaleProvider>
```

当某个对话框需要自定义文案时，可在 `Content` 上使用 `closeLabel`，或在 `Actions` 上使用 `dismissLabel`；显式属性会覆盖提供者的默认值。

## 桌面端宽度
桌面端对话框默认使用 `size="base"`（576px）。当内容需要不同宽度时，可使用 `sm`（448px）、`lg`（672px）或 `xl`（768px）。移动端对话框在所有尺寸下都保持全宽。
<ComponentExample demo="LayerDialogSizeDemo"><LayerDialogSizeDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 桌面端位置
桌面端对话框默认居中。仅当内容适合顶部对齐时，才使用 `verticalAlign="top"` 这一窄化覆盖项；移动端仍是底部抽屉（bottom sheet）。
<ComponentExample demo="LayerDialogTopAlignDemo"><LayerDialogTopAlignDemo client:load /></ComponentExample>
</ComponentSection>

<ComponentSection>
## 最大高度
高度由内容决定，并以视口为上限。遵循 Base UI 的 inside-scroll 模式，视口为每种位置预留垂直空间，弹出层填满该空间，因此顶部对齐的对话框永远不会超出底部边缘。超过上限后，只有正文滚动，标题栏和操作栏保持固定。移动端抽屉则以视口的 85% 为上限。不存在高度属性：使用方只能通过所渲染的内容来控制高度。
<ComponentExample demo="LayerDialogMaxHeightDemo"><LayerDialogMaxHeightDemo client:load /></ComponentExample>
</ComponentSection>

{/* API Reference */}

<ComponentSection>

## API 参考

### LayerDialog.Root

<p>
  控制打开状态。接受 Base UI Drawer 的所有 root 属性，包括 `open`、
  `defaultOpen`、`onOpenChange`、`modal` 和 `actionsRef`。不渲染自身的
  HTML 元素。
</p>
<div class="mb-4 overflow-hidden rounded-lg border border-kumo-hairline">
  <table class="w-full text-sm">
    <thead class="bg-kumo-elevated">
      <tr>
        <th class="px-4 py-2 text-left font-medium">属性</th>
        <th class="px-4 py-2 text-left font-medium">类型</th>
        <th class="px-4 py-2 text-left font-medium">默认值</th>
        <th class="px-4 py-2 text-left font-medium">描述</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-kumo-hairline">
      <tr>
        <td class="px-4 py-2 font-mono text-xs">dismissDisabled</td>
        <td class="px-4 py-2 font-mono text-xs">boolean</td>
        <td class="px-4 py-2 font-mono text-xs">false</td>
        <td class="px-4 py-2 text-kumo-subtle">
          在操作待处理期间，阻止通过 X、关闭、Escape、背景和滑动进行关闭。
          程序化关闭永远不会被阻止。
        </td>
      </tr>
    </tbody>
  </table>
</div>

### LayerDialog.Alert

<p>
  与 `LayerDialog.Root` 属性相同。强制 `modal`、阻止指针关闭、渲染
  `role="alertdialog"`，并要求使用 `LayerDialog.Actions`。
</p>

### LayerDialog.Trigger

<p>点击后打开对话框的按钮。</p>
<PropsTable component="LayerDialog.Trigger" />

### LayerDialog.Content

<p>将背景和弹出层放入 portal，并校验组合的合法性。</p>
<PropsTable component="LayerDialog.Content" />

### LayerDialog.Title

<p>为对话框提供无障碍标签的标题。</p>
<PropsTable component="LayerDialog.Title" />

### LayerDialog.Description

<p>
  标题下方的可选说明文字，会成为对话框的可访问描述。
</p>
<PropsTable component="LayerDialog.Description" />

### LayerDialog.Body

<p>可滚动的内容区域。</p>
<PropsTable component="LayerDialog.Body" />

### LayerDialog.Actions

<p>
  包含一个自动关闭按钮和恰好一个 `LayerDialog.Action` 的底部操作栏。
</p>
<PropsTable component="LayerDialog.Actions" />

### LayerDialog.Action

<p>
  唯一的主操作。渲染一个 Kumo `Button`，并接受 `onClick`、`disabled` 和 `type`
  等标准按钮属性。通过 `menu` 传入菜单项，可渲染匹配的拆分按钮菜单。
</p>
<PropsTable component="LayerDialog.Action" />

</ComponentSection>
