---
layout: ~/layouts/MdxDocLayout.astro
title: "CodeHighlighted"
description: "基于 Shiki 的语法高亮组件，具备 VS Code 级别的高亮质量、主题支持与按需加载。"
sourceFile: "code"
---

import ComponentExample from "~/components/docs/ComponentExample.astro";
import ComponentSection from "~/components/docs/ComponentSection.astro";
import {
  CodeHighlightedBasicDemo,
  CodeHighlightedTypeScriptDemo,
  CodeHighlightedReactDemo,
  CodeHighlightedBashDemo,
  CodeHighlightedJsonDemo,
  CodeHighlightedHighlightLinesDemo,
  CodeHighlightedLineNumbersDemo,
  CodeHighlightedCopyButtonDemo,
  CodeHighlightedFullFeaturedDemo,
  CodeHighlightedSharedProviderDemo,
  CodeHighlightedCssDemo,
  CodeHighlightedCustomHighlightDemo,
} from "~/components/demos/CodeHighlightedDemo";

{/* Hero Demo */}

<ComponentSection>
  <div class="mb-4 rounded-md border border-kumo-brand/20 bg-kumo-brand/5 p-4">
    <p class="text-sm text-kumo-strong">
      <strong>v1.10 新增：</strong>基于 Shiki 的语法高亮，支持按需
      加载。请从 `@cloudflare/kumo/code` 导入使用。
    </p>
  </div>
  <ComponentExample demo="CodeHighlightedBasicDemo">
    <CodeHighlightedBasicDemo client:load />
  </ComponentExample>
</ComponentSection>

{/* Overview */}

<ComponentSection>

## 概述

  <p>
    基于 Shiki 的语法高亮组件。支持 200+ 种语言（基于 TextMate 语法）、浅色/深色双主题，并按需加载。通过独立的入口（ `@cloudflare/kumo/code` ）导出，避免不需要它的应用打包 Shiki。
  </p>
</ComponentSection>

{/* Installation */}

<ComponentSection>

## 安装

  <p>
    CodeHighlighted 从独立的入口导出，避免不需要它的应用打包 Shiki。
  </p>

```tsx
import { ShikiProvider, CodeHighlighted } from "@cloudflare/kumo/code";
```

  <div class="mt-4 rounded-md border border-kumo-warning/20 bg-kumo-warning/5 p-4">
    <p class="text-sm text-kumo-strong">
      <strong>重要：</strong>不要从主入口 `@cloudflare/kumo` 导入。
      否则即使你没有使用 Shiki，也会把它打包进你的产物中。
    </p>
  </div>
</ComponentSection>

{/* Basic Usage */}

<ComponentSection>

## 基本用法

  <p>
    使用 `ShikiProvider` 包裹你的应用，只需一次性配置 Shiki。
    所有 `CodeHighlighted` 组件共享同一个 Shiki 实例。
  </p>

```tsx
import { ShikiProvider, CodeHighlighted } from "@cloudflare/kumo/code";

export function App() {
  return (
    <ShikiProvider
      engine="javascript"
      languages={["tsx", "typescript", "bash", "json"]}
    >
      {/* All CodeHighlighted components share the same Shiki instance */}
      <CodeHighlighted code="const x = 1;" lang="typescript" />
    </ShikiProvider>
  );
}
```

</ComponentSection>

{/* Examples */}

<ComponentSection>

## 示例

### 语言

<p>
  CodeHighlighted 通过 Shiki 支持 200+ 种语言。只加载你需要的语言即可。
</p>

#### TypeScript

<ComponentExample demo="CodeHighlightedTypeScriptDemo">
  <CodeHighlightedTypeScriptDemo client:visible />
</ComponentExample>

#### React / TSX

<ComponentExample demo="CodeHighlightedReactDemo">
  <CodeHighlightedReactDemo client:visible />
</ComponentExample>

#### Bash / Shell

<ComponentExample demo="CodeHighlightedBashDemo">
  <CodeHighlightedBashDemo client:visible />
</ComponentExample>

#### JSON

<ComponentExample demo="CodeHighlightedJsonDemo">
  <CodeHighlightedJsonDemo client:visible />
</ComponentExample>

#### CSS

<ComponentExample demo="CodeHighlightedCssDemo">
  <CodeHighlightedCssDemo client:visible />
</ComponentExample>

### 高亮行

<p>使用 `highlightLines`（从 1 开始索引）强调特定行。</p>
<ComponentExample demo="CodeHighlightedHighlightLinesDemo">
  <CodeHighlightedHighlightLinesDemo client:visible />
</ComponentExample>

### 自定义高亮颜色

<p>
  使用 `--kumo-code-highlight-bg` CSS 变量自定义高亮颜色。
</p>
<CodeHighlightedCustomHighlightDemo client:load />

### 行号

<p>使用 `showLineNumbers` 显示行号。</p>
<ComponentExample demo="CodeHighlightedLineNumbersDemo">
  <CodeHighlightedLineNumbersDemo client:visible />
</ComponentExample>

### 复制按钮

<p>使用 `showCopyButton` 添加复制到剪贴板的按钮。</p>
<ComponentExample demo="CodeHighlightedCopyButtonDemo">
  <CodeHighlightedCopyButtonDemo client:visible />
</ComponentExample>

### 完整功能

<p>组合全部功能，带来完整的代码展示体验。</p>
<ComponentExample demo="CodeHighlightedFullFeaturedDemo">
  <CodeHighlightedFullFeaturedDemo client:visible />
</ComponentExample>

### 共享 Provider

  <p>
    多个代码块可以共享同一个 `ShikiProvider`。
    Shiki 只加载一次，并供所有代码块复用。
  </p>
  <ComponentExample demo="CodeHighlightedSharedProviderDemo">
    <CodeHighlightedSharedProviderDemo client:visible />
  </ComponentExample>
</ComponentSection>

{/* Themes */}

<ComponentSection>

## 主题

  <p>
    CodeHighlighted 使用内置主题，保证所有 Kumo 应用样式一致：
  </p>
  <ul class="mb-4 list-disc pl-6">
    <li>
      <strong>浅色模式：</strong>
      `github-light`
    </li>
    <li>
      <strong>深色模式：</strong>
      `vesper`
    </li>
  </ul>
  <p class="text-sm text-kumo-subtle">
    不支持自定义主题。这能确保应用中所有代码块视觉一致。
  </p>
</ComponentSection>

{/* Server-Side Usage */}

<ComponentSection>

## 服务端用法

  <p>
    对于 SSR 框架（Next.js RSC、Astro、Remix），请在构建时使用服务端工具进行高亮。
  </p>

#### 一次性高亮

```tsx
// Next.js RSC or Astro
import { highlightCode } from "@cloudflare/kumo/code/server";

export default async function Page() {
  const html = await highlightCode(`const x = 1;`, "typescript");

  return <pre dangerouslySetInnerHTML={{ __html: html }} />;
}
```

#### 可复用高亮器

```tsx
// For multiple highlights, reuse the highlighter
import { createServerHighlighter } from "@cloudflare/kumo/code/server";

const highlighter = await createServerHighlighter({
  languages: ["tsx", "bash", "json"],
});

const html1 = highlighter.highlight(code1, "tsx");
const html2 = highlighter.highlight(code2, "bash");

highlighter.dispose(); // Clean up when done
```

</ComponentSection>

{/* Custom Hook */}

<ComponentSection>

## 自定义 Hook

  <p>
    使用 `useShikiHighlighter` 实现自定义场景。
  </p>

```tsx
import { useShikiHighlighter } from "@cloudflare/kumo/code";

function CustomCodeBlock({ code, lang }) {
  const { highlight, isLoading, isReady, error } = useShikiHighlighter();

  if (error) {
    return <div className="text-red-500">Failed to load highlighter</div>;
  }

  if (isLoading) {
    return (
      <pre className="animate-pulse">
        <code>{code}</code>
      </pre>
    );
  }

  const html = highlight(code, lang);

  // null means highlighting failed — render plain text
  if (html === null) {
    return (
      <pre>
        <code>{code}</code>
      </pre>
    );
  }

  return <pre dangerouslySetInnerHTML={{ __html: html }} />;
}
```

</ComponentSection>

{/* Internationalization */}

<ComponentSection>

## 国际化

  <p>
    可在 provider 层级为所有代码块统一设置按钮文案，也可在单个组件上覆盖。
  </p>

```tsx
// Set labels at the provider level for all code blocks
<ShikiProvider
  engine="javascript"
  languages={["tsx", "bash"]}
  labels={{ copy: "Copier", copied: "Copié!" }}
>
  <App />
</ShikiProvider>

// Or override at the component level
<CodeHighlighted
  code={code}
  lang="tsx"
  showCopyButton
  labels={{ copy: "Copy code", copied: "Done!" }}
/>
```

</ComponentSection>

{/* Framework Integration */}

<ComponentSection>

## 框架集成

### Next.js App Router

```tsx
// app/providers.tsx
"use client";

import { ShikiProvider } from "@cloudflare/kumo/code";

export function Providers({ children }) {
  return (
    <ShikiProvider engine="javascript" languages={["tsx", "bash", "json"]}>
      {children}
    </ShikiProvider>
  );
}

// app/layout.tsx
import { Providers } from "./providers";

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
```

### Astro（静态）

<p>
  对于静态站点，使用服务端高亮可以实现零客户端 JavaScript。
</p>

```tsx
---
// src/components/CodeBlock.astro
import { highlightCode } from "@cloudflare/kumo/code/server";

const { code, lang } = Astro.props;
const html = await highlightCode(code, lang);
---

<div class="code-block" set:html={html} />
```

</ComponentSection>

{/* Bundle Size */}

<ComponentSection>

## 体积占用

  <p>
    Shiki 在首次渲染时按需加载。体积取决于你的配置：
  </p>
  <div class="overflow-x-auto">
    <table class="w-full text-sm">
      <thead>
        <tr class="border-b border-kumo-hairline">
          <th class="py-2 pr-4 text-left font-medium">场景</th>
          <th class="py-2 pr-4 text-left font-medium">语言</th>
          <th class="py-2 pr-4 text-left font-medium">引擎</th>
          <th class="py-2 text-left font-medium">按需加载体积</th>
        </tr>
      </thead>
      <tbody>
        <tr class="border-b border-kumo-hairline/50">
          <td class="py-2 pr-4">最小配置</td>
          <td class="py-2 pr-4">tsx, json</td>
          <td class="py-2 pr-4">JS</td>
          <td class="py-2">~75 KB</td>
        </tr>
        <tr class="border-b border-kumo-hairline/50">
          <td class="py-2 pr-4">标准配置</td>
          <td class="py-2 pr-4">tsx, ts, bash, json, css, yaml</td>
          <td class="py-2 pr-4">JS</td>
          <td class="py-2">~95 KB</td>
        </tr>
        <tr>
          <td class="py-2 pr-4">完整配置</td>
          <td class="py-2 pr-4">15+ 种语言</td>
          <td class="py-2 pr-4">WASM</td>
          <td class="py-2">~250 KB</td>
        </tr>
      </tbody>
    </table>
  </div>
  <p class="mt-4 text-sm text-kumo-subtle">
    不引入 `@cloudflare/kumo/code` 的应用额外体积为 0 KB。
  </p>
</ComponentSection>

{/* Migration */}

<ComponentSection>

## 从 Code/CodeBlock 迁移

  <p>
    旧版 `Code` 和 `CodeBlock` 组件已废弃。
    它们将在 v2.0 中移除。
  </p>

```tsx
// Before (deprecated)
import { Code, CodeBlock } from "@cloudflare/kumo";
<CodeBlock code="const x = 1;" lang="ts" />

// After
import { ShikiProvider, CodeHighlighted } from "@cloudflare/kumo/code";

// Once at app root
<ShikiProvider engine="javascript" languages={["tsx"]}>
  <App />
</ShikiProvider>

// In components
<CodeHighlighted code="const x = 1;" lang="tsx" />
```

</ComponentSection>

{/* API Reference */}

<ComponentSection>

## API 参考

### ShikiProvider 属性

<div class="overflow-x-auto">
  <table class="w-full text-sm">
    <thead>
      <tr class="border-b border-kumo-hairline">
        <th class="py-2 pr-4 text-left font-medium">属性</th>
        <th class="py-2 pr-4 text-left font-medium">类型</th>
        <th class="py-2 pr-4 text-left font-medium">必填</th>
        <th class="py-2 text-left font-medium">说明</th>
      </tr>
    </thead>
    <tbody>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>engine</code>
        </td>
        <td class="py-2 pr-4">
          <code>"javascript" | "wasm"</code>
        </td>
        <td class="py-2 pr-4">是</td>
        <td class="py-2">
          JS 体积更小（约 50KB），WASM 更准确（约 180KB）
        </td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>languages</code>
        </td>
        <td class="py-2 pr-4">
          <code>string[]</code>
        </td>
        <td class="py-2 pr-4">是</td>
        <td class="py-2">要支持的语言（如 ["tsx", "bash"]）</td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>labels</code>
        </td>
        <td class="py-2 pr-4">
          <code>{"{ copy?: string, copied?: string }"}</code>
        </td>
        <td class="py-2 pr-4">否</td>
        <td class="py-2">复制按钮的本地化文案</td>
      </tr>
      <tr>
        <td class="py-2 pr-4">
          <code>children</code>
        </td>
        <td class="py-2 pr-4">
          <code>ReactNode</code>
        </td>
        <td class="py-2 pr-4">是</td>
        <td class="py-2">应用内容</td>
      </tr>
    </tbody>
  </table>
</div>

### CodeHighlighted 属性

<div class="overflow-x-auto">
  <table class="w-full text-sm">
    <thead>
      <tr class="border-b border-kumo-hairline">
        <th class="py-2 pr-4 text-left font-medium">属性</th>
        <th class="py-2 pr-4 text-left font-medium">类型</th>
        <th class="py-2 pr-4 text-left font-medium">必填</th>
        <th class="py-2 text-left font-medium">说明</th>
      </tr>
    </thead>
    <tbody>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>code</code>
        </td>
        <td class="py-2 pr-4">
          <code>string</code>
        </td>
        <td class="py-2 pr-4">是</td>
        <td class="py-2">要展示的源代码</td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>lang</code>
        </td>
        <td class="py-2 pr-4">
          <code>string</code>
        </td>
        <td class="py-2 pr-4">是</td>
        <td class="py-2">
          语言标识（必须在 provider 的 languages 中）
        </td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>showLineNumbers</code>
        </td>
        <td class="py-2 pr-4">
          <code>boolean</code>
        </td>
        <td class="py-2 pr-4">否</td>
        <td class="py-2">显示行号</td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>highlightLines</code>
        </td>
        <td class="py-2 pr-4">
          <code>number[]</code>
        </td>
        <td class="py-2 pr-4">否</td>
        <td class="py-2">要强调的行（从 1 开始索引）</td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>showCopyButton</code>
        </td>
        <td class="py-2 pr-4">
          <code>boolean</code>
        </td>
        <td class="py-2 pr-4">否</td>
        <td class="py-2">显示复制到剪贴板的按钮</td>
      </tr>
      <tr class="border-b border-kumo-hairline/50">
        <td class="py-2 pr-4">
          <code>labels</code>
        </td>
        <td class="py-2 pr-4">
          <code>{"{ copy?: string, copied?: string }"}</code>
        </td>
        <td class="py-2 pr-4">否</td>
        <td class="py-2">覆盖此实例的 provider 文案</td>
      </tr>
      <tr>
        <td class="py-2 pr-4">
          <code>className</code>
        </td>
        <td class="py-2 pr-4">
          <code>string</code>
        </td>
        <td class="py-2 pr-4">否</td>
        <td class="py-2">额外的 CSS 类</td>
      </tr>
    </tbody>
  </table>
</div>

### useShikiHighlighter 返回值

  <div class="overflow-x-auto">
    <table class="w-full text-sm">
      <thead>
        <tr class="border-b border-kumo-hairline">
          <th class="py-2 pr-4 text-left font-medium">属性</th>
          <th class="py-2 pr-4 text-left font-medium">类型</th>
          <th class="py-2 text-left font-medium">说明</th>
        </tr>
      </thead>
      <tbody>
        <tr class="border-b border-kumo-hairline/50">
          <td class="py-2 pr-4"><code>highlight</code></td>
          <td class="py-2 pr-4"><code>(code, lang, options?) =&gt; string | null</code></td>
          <td class="py-2">返回高亮后的 HTML；未就绪时返回 null</td>
        </tr>
        <tr class="border-b border-kumo-hairline/50">
          <td class="py-2 pr-4"><code>isLoading</code></td>
          <td class="py-2 pr-4"><code>boolean</code></td>
          <td class="py-2">Shiki 加载期间为 true</td>
        </tr>
        <tr class="border-b border-kumo-hairline/50">
          <td class="py-2 pr-4"><code>isReady</code></td>
          <td class="py-2 pr-4"><code>boolean</code></td>
          <td class="py-2">当 highlight() 可安全调用时为 true</td>
        </tr>
        <tr>
          <td class="py-2 pr-4"><code>error</code></td>
          <td class="py-2 pr-4"><code>Error | null</code></td>
          <td class="py-2">Shiki 初始化失败时返回错误</td>
        </tr>
      </tbody>
    </table>
  </div>
</ComponentSection>
