---
layout: ~/layouts/MdxDocLayout.astro
title: "Autocomplete"
description: "一个自由文本输入框，附带可选的过滤建议列表。与 Combobox 不同，其值不受限于选项列表。"
sourceFile: "components/autocomplete"
baseUIComponent: "autocomplete"
---

import ComponentExample from "~/components/docs/ComponentExample.astro";
import ComponentSection from "~/components/docs/ComponentSection.astro";
import CodeBlock from "~/components/docs/CodeBlock.astro";
import PropsTable from "~/components/docs/PropsTable.astro";
import {
  AutocompleteDemo,
  AutocompleteWithFieldDemo,
  AutocompleteErrorDemo,
  AutocompleteGroupedDemo,
  AutocompleteSizesDemo,
  AutocompleteControlledDemo,
} from "~/components/demos/AutocompleteDemo";

{/* Hero Demo */}

<ComponentSection>
  <ComponentExample demo="AutocompleteDemo">
    <AutocompleteDemo client:load />
  </ComponentExample>
</ComponentSection>

{/* Installation */}

<ComponentSection>

## 安装

### 批量导入

<CodeBlock
  code={`import { Autocomplete } from "@cloudflare/kumo";`}
  lang="tsx"
/>

### 按需导入

  <CodeBlock
    code={`import { Autocomplete } from "@cloudflare/kumo/components/autocomplete";`}
    lang="tsx"
  />
</ComponentSection>

{/* When to use */}

<ComponentSection>

## 适用场景

<p>
  当输入值可以是自由文本、建议仅为可选提示时，使用 `Autocomplete`。当选中的值必须来自预定义列表时，改用 `Combobox`。
</p>
</ComponentSection>

{/* Controlled */}

<ComponentSection>

## 受控

<p>
  传入 `value` 和 `onValueChange` 以实现受控用法。
</p>
<ComponentExample demo="AutocompleteControlledDemo">
  <AutocompleteControlledDemo client:load />
</ComponentExample>
</ComponentSection>

{/* With Field */}

<ComponentSection>

## 配合 Field

<p>
  添加 `label`、`description` 和 `required` 以启用内置的 Field 包装器。
</p>
<ComponentExample demo="AutocompleteWithFieldDemo">
  <AutocompleteWithFieldDemo client:load />
</ComponentExample>
</ComponentSection>

{/* Error State */}

<ComponentSection>

## 错误状态

<p>
  通过 `error` 属性展示校验错误。
</p>
<ComponentExample demo="AutocompleteErrorDemo">
  <AutocompleteErrorDemo client:load />
</ComponentExample>
</ComponentSection>

{/* Grouped */}

<ComponentSection>

## 分组

<p>
  使用 `Autocomplete.Group` 和 `Autocomplete.GroupLabel` 将项目按类别分组。
</p>
<ComponentExample demo="AutocompleteGroupedDemo">
  <AutocompleteGroupedDemo client:load />
</ComponentExample>
</ComponentSection>

{/* Sizes */}

<ComponentSection>

## 尺寸

<p>
  `Autocomplete.InputGroup` 上的 `size` 属性支持与 Input 组件一致的四种变体：`xs`、`sm`、`base`（默认）和 `lg`。
</p>
<ComponentExample demo="AutocompleteSizesDemo">
  <AutocompleteSizesDemo client:load />
</ComponentExample>
</ComponentSection>

{/* Filtering */}

<ComponentSection>

## 过滤

<p>
  默认情况下过滤不区分大小写和重音符号，底层由 `Intl.Collator` 实现。对于字符串项，无需自定义 `filter`。
</p>
<p>
  当需要对对象项的某个属性进行过滤时，使用 `Autocomplete.useFilter()` 以保留内置的不区分重音符号的匹配行为：
</p>

```tsx
function LanguagePicker() {
  const { contains } = Autocomplete.useFilter();

  const filter = useCallback(
    (item: Language, query: string) => contains(item.label, query),
    [contains],
  );

  return (
    <Autocomplete items={languages} filter={filter}>
      {/* ... */}
    </Autocomplete>
  );
}
```

<p>
  若要完全禁用过滤（例如结果来自服务器时），传入 `filter={null}`：
</p>

```tsx
<Autocomplete items={results} filter={null}>
  ...
</Autocomplete>
```

</ComponentSection>

{/* API Reference */}

<ComponentSection>

## API 参考

### Autocomplete

<p>根组件。包装所有子组件并管理状态。</p>
<PropsTable component="Autocomplete" />

### Autocomplete.InputGroup

<p>
  自包含的输入包装器，将文本输入框、清除按钮和下拉触发器渲染在一起。
</p>
<PropsTable component="Autocomplete.InputGroup" />

### Autocomplete.Content

<p>下拉弹层容器。包装 Portal、Positioner 和 Popup。</p>
<PropsTable component="Autocomplete.Content" />

### Autocomplete.Item

<p>列表中的单个建议项。</p>
<PropsTable component="Autocomplete.Item" />

### 其他子组件

  <ul class="list-disc list-inside space-y-1">
    <li>`Autocomplete.List` — 带 render prop 的可滚动列表容器</li>
    <li>`Autocomplete.Group` — 将项目归并到某个标题下</li>
    <li>`Autocomplete.GroupLabel` — 分组的标题标签</li>
    <li>`Autocomplete.Collection` — 组内的项目容器</li>
    <li>`Autocomplete.Separator` — 项目之间的水平分隔线</li>
  </ul>
</ComponentSection>
